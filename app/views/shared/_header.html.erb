<header>
  <div class="title">
    <%= link_to  'Testguru', tests_path, method: :get %>
  </div>
  <div class="right-nav-bar">
      <% if user_signed_in? %>
      <p><%= t('.welcome', email: current_user.email) %></b>|
        <%= button_to  t('.logout'), destroy_user_session_path, method: :delete %></p>
      <% else %>
      <%= link_to  t('.sign_up'), new_user_registration_path %>
      <% end %>
      </div>
      </header>