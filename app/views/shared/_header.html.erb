<nav class="navbar navbar-light bg-light">
  <h1>
    <%= link_to  "Testguru", tests_path, class: 'navbar-brand'%>
    <% if current_user&.admin? %>
      <span class="badge badge-secondary"><%= link_to  t('.admin_panel'), admin_tests_path, class: 'navbar-brand'%></span>
    <% end %>
    <%= link_to t('.feedback'), new_feedback_form_path, class: 'navbar-brand' %>
  </h1>
  <span class="navbar-text">
      <% if user_signed_in? %>
      <%= t('.welcome', email: current_user.email) %></b> |
      <%= button_to  t('.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to  t('.log_in'), new_user_session_path %></b> |
      <%= link_to  t('.sign_up'), new_user_registration_path %>
      <% end %>
      </span>
</nav>